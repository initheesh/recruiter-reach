{
  "name": "recruiter-reach",
  "version": "1.0.0",
  "description": "AI-powered recruiter outreach app that tailors job application emails from your resume, lets you edit/send them via Gmail, and tracks sent requests with preview/history.",
  "license": "MIT",
  "author": "Nitheesh Shaju",
  "type": "commonjs",
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "install:backend": "cd backend && uv sync",
    "install:all": "npm run install:frontend && npm run install:backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && uv run uvicorn main:app --reload",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
